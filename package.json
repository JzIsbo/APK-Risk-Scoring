{
  "name": "apk-risk-score-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:backend": "composer install --working-dir=backend",
    "install:frontend": "npm install --prefix frontend",
    "install:all": "npm run install:backend && npm run install:frontend",
    "dev:backend": "cd backend && php artisan serve --port=8000",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "npx concurrently -n \"backend,frontend\" -c \"cyan,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\" --kill-others"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
